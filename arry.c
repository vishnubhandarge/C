#include <stdio.h>

int main()
{
    int marks[5], i;
    int sum=0;
    int average;

      printf("enter the marks: ");

      for(i=0; i<5; i++);
      {
          scanf("%d", &marks[i]);
      }

    return 0;

}
